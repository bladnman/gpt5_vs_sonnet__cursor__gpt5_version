# Database Configuration
# URL for your database connection (SQLite for development, PostgreSQL for production)
DATABASE_URL="file:./prisma/dev.db"

# TMDB (The Movie Database) API Configuration
# Get your API key from: https://www.themoviedb.org/settings/api
# You can use either the v3 API key OR the v4 access token (v4 is recommended for newer features)

# TMDB API v3 Key (legacy, used as query parameter)
TMDB_API_KEY="your_tmdb_v3_api_key_here"

# TMDB API v4 Access Token (recommended, used as Bearer token)
# This provides access to newer TMDB features and better rate limits
TMDB_V4_TOKEN="your_tmdb_v4_access_token_here"

# Redis Configuration (optional)
# URL for Redis cache (leave empty if not using Redis caching)
REDIS_URL=""
